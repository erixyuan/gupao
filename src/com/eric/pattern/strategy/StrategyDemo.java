package com.eric.pattern.strategy;


/**
 *
 * 策略模式(Strategy Pattern)是指定义了算法家族、分别封 装起来，让它们之间可以互相替换，此模式让算法的变化不会 影响到使用算法的用户。
 *
 * 例子：
 * 支付方式的选择
 *
 * 缺点：
 * 1、客户端必须知道所有的策略，并且自行决定使用哪个策略类
 * 2、代理中会产生非常多策略类，增加维护难度
 *
 */
public class StrategyDemo {
}
